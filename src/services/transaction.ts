import api from "../utils/api";

// export const getApprovalKegiatan = async (param?: object): Promise<any> => {
//   let params = {};
//   if (param) params = { queryParam: JSON.stringify(param) };
//   const response = await api.get<any>("/v2/planning-services/usulan-rencana-kegiatan");
//   let tmp0: any = response.data.return;
//   localStorage.setItem("approval", JSON.stringify(tmp0));
//   return response.data.return;
// };
